{
  "name": "tankaicore-cloudflare",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:types": "wrangler types",
    "cf:check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "cf:deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2",
    "wrangler": "4.124.0"
  }
}
